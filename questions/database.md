#### 1.时序数据库和非时序数据库有何区别

* 数据模型：时序数据库中时间戳是必带字段，非时序数据库中时间只是普通的字段
* 写入特点：时序数据库数据按时间分片超高并发写入，基本都是追加，少更新/删除。非时序数据库读写均衡，大量更新/删除/事务
* 查询特点：时序数据库查询基本都带时间范围，以聚合查询为主。非时序数据库多条件/联表查询
* 存储特点：时序数据库以时间分区，按列存储，高压缩比，支持自动过期。非时序数据库以行/文档存储，低压缩比，数据长期保存。
* 适用场景：时序数据库优化时间线数据，多用于监控数据。非时序数据库面向通用业务，多用于用户/订单系统。